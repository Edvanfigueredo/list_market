const cache=new Map();export function getProductCanvas(product){if(cache.has(product.id))return cache.get(product.id);const size=112,canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const ctx=canvas.getContext('2d');ctx.fillStyle=product.bg;ctx.fillRect(0,0,size,size);const gradient=ctx.createRadialGradient(size/2,size/2,size*.1,size/2,size/2,size*.6);gradient.addColorStop(0,'rgba(255,255,255,.08)');gradient.addColorStop(1,'rgba(0,0,0,.35)');ctx.fillStyle=gradient;ctx.fillRect(0,0,size,size);ctx.font=`${Math.round(size*.52)}px serif`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(product.emoji,size/2,size/2+size*.04);cache.set(product.id,canvas);return canvas}
