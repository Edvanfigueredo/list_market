import {changeQuantity,saveAll,setPrice} from './state/store.js';import {renderList,updateCard} from './ui/renderer.js';import {updateProgress} from './ui/progress.js';import {setupTabs} from './ui/tabs.js';import {showToast} from './ui/toast.js';
function refreshItem(id){updateCard(id,true);updateProgress()}function changeItemQuantity(id,delta){changeQuantity(id,delta);refreshItem(id)}function changeItemPrice(id,price){setPrice(id,price);refreshItem(id)}function renderMarket(){renderList('list-mercado',true,changeItemQuantity,changeItemPrice);updateProgress()}function setupSaveButton(){document.getElementById('saveBtn').addEventListener('click',()=>{saveAll();const button=document.getElementById('saveBtn');button.textContent='✅ LISTA SALVA!';button.classList.add('saved');showToast('💾 LISTA SALVA COM SUCESSO!');setTimeout(()=>{button.textContent='💾 SALVAR LISTA';button.classList.remove('saved')},2500)})}renderList('list-planejar',false,changeItemQuantity,changeItemPrice);setupTabs(renderMarket);setupSaveButton();updateProgress();
