import {products,state} from '../state/store.js';import {formatCurrency,toNumber} from '../utils/currency.js';
export function updateProgress(){const total=products.length,done=products.filter(product=>state[product.id].done).length,grandTotal=products.reduce((sum,product)=>sum+toNumber(state[product.id].price)*state[product.id].qty,0);document.getElementById('totalItems').textContent=total;document.getElementById('doneItems').textContent=done;document.getElementById('grandTotal').textContent=formatCurrency(grandTotal);const percent=Math.round(done/total*100);document.getElementById('progressFill').style.width=`${percent}%`;document.getElementById('progressText').textContent=`${percent}%`;document.getElementById('t-itens').textContent=total;document.getElementById('t-coletados').textContent=done;document.getElementById('t-total').textContent=`R$ ${formatCurrency(grandTotal)}`}
