export function setupItemForm(onAdd){const form=document.getElementById('addItemForm');form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const name=data.get('name').trim();if(!name)return;onAdd({name,description:data.get('description'),emoji:data.get('emoji')});form.reset();document.getElementById('itemEmoji').value='🛒';document.getElementById('itemName').focus()})}
